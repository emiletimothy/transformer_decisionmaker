You are running the Multiplicative Weights Update algorithm with {n} experts and learning rate eta = {eta}.

Definitions:
- expert_predictions[i] is the binary prediction (0 or 1) of expert i at this step.
- true_label is the correct binary label for this step (revealed after your prediction).
- loss[i] = 1 if expert_predictions[i] != true_label, else loss[i] = 0.

Initialize weights uniformly: w[i] = 1/{n} for all i.

At each step:
1. Compute the mixture probability: q = sum_i w[i] * expert_predictions[i].
2. Output your binary prediction: yhat = 1 if q >= 0.5, else yhat = 0.
3. After I reveal the true label, compute losses and update weights:
   w[i] <- w[i] * exp(-eta * loss[i]) for each i, then normalize so that sum(w) = 1.

I will give you expert predictions one step at a time. After you output your prediction I will reveal the true label; then I will give you the next step's predictions.

Output valid JSON only — no explanation, no reasoning steps, no other text:
{
  "weights": [...],
  "mixture_probability": <float>,
  "prediction": <0 or 1>
}
where "weights" are your current weights before this step's update.
