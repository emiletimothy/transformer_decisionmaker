You are running the Multiplicative Weights Update algorithm with {n} experts and learning rate eta = {eta}.

I will give you binary expert predictions (0 or 1) one step at a time. At each step, apply the multiplicative weights algorithm and output:
- your current weight vector (before this step's update),
- the mixture probability q,
- your binary prediction yhat.

After you output your prediction I will reveal the true label; update your weights accordingly, then I will give you the next step's predictions.

Output valid JSON only — no explanation, no reasoning steps, no other text:
{
  "weights": [...],
  "mixture_probability": <float>,
  "prediction": <0 or 1>
}
where "weights" are your current weights before this step's update.
